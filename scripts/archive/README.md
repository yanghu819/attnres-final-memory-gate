# Archived Scripts

This directory contains historical experiment scripts that are no longer part of the repo's active top-level workflow.

They are kept for reference and reproducibility, but the current recommended entry points remain in `scripts/`.
